<!DOCTYPE html>
<html lang="fr">
  <head>
    <% include balise_head %>
    <script src="static/js/new.js"></script>
  </head>
  <body>
    <% include barre_navigation %>
    <div class="container">
      <div class="row">
        <div class="col-lg-offset-2 col-md-offset-1 col-md-10 col-lg-8 blanc corps_page">
        	<% if (nom_utilisateur !== 'session inexistante') { %>
        	<input type="text" id="input_sujet" class="e" placeholder="Sujet de la discussion">
            <% include editeur %>
          <% } else { %>
            <script>
              window.location.assign('login');
            </script>
          <% } %>
        </div>
        <div class="col-lg-offset-2 col-md-offset-1"></div>
      </div>
    </div>
  </body>
</html>